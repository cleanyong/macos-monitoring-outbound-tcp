# check all the outbound TCP on macos, to see any app suspecious, the entries will save to a file in currect folder: mac-monitoring-stats.log
```
cargo run
```

# additional can check udp as well
```
cargo run -- udp
```
